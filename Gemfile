source :rubygems
gem 'sinatra', '1.3.2'
gem 'slim'
gem 'data_mapper'

group :production do
    gem 'pg'
    gem 'dm-postgres-adapter'
end

group :development, :test do
    gem 'sqlite3'
    gem 'dm-sqlite-adapter'
end
